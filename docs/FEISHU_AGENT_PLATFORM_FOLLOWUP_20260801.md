# GitLink 飞书 Review 机器人第二轮平台问题

日期：2026-08-01

> 2026-08-01：飞书 Agent 的第二轮答复已收到。答复中的权限、卡片 ACK、Task v2、
> Base 事件和重试周期存在需要纠正的内容，不能整体作为实现合同。逐项核验见
> `FEISHU_AGENT_PLATFORM_RESPONSE_AUDIT_20260801.md`。

请尽量对每个问题提供：

```text
明确结论
官方接口或 SDK 方法
准确的权限 Scope
请求或事件示例
平台限制
官方文档链接
```

不要只给架构建议。

## Q1：请纠正并给出最小权限的准确 Scope

上一轮给出的权限包含：

```text
im:message:receive_v1
im:message:message:readonly
im:chat:readonly
im:chat.member:readonly
im:message:send_as_bot
```

其中 `im.message.receive_v1` 更像事件类型，不是权限 Scope；其余名称也可能是旧版本或不准确名称。

请给出以下六种场景的准确权限：

1. 接收群聊中用户 `@机器人` 的消息；
2. 读取消息正文、sender 和 mention；
3. 回复原消息；
4. 主动向群发送消息；
5. 获取群名称和群主 `owner_id`；
6. 接收交互卡片按钮回调。

请明确区分：

```text
事件类型
应用身份权限
用户身份权限
是否需要发布新版本
是否需要企业管理员审批
```

## Q2：交互卡片按钮回调的真实时限是多少

上一轮回答称：

```text
卡片按钮点击后必须在 3 秒内更新卡片
否则客户端显示操作失败
```

请确认：

1. 3 秒限制针对什么请求？
2. 是必须完成业务处理，还是只需返回成功确认？
3. Channel SDK 长连接中的卡片回调如何立即 ACK？
4. ACK 后是否可以异步更新原卡片？
5. 异步更新原卡片需要哪个 OpenAPI？
6. 是否需要保存 `message_id`、`card_id` 或其他 Token？
7. 回调失败时飞书是否重试？
8. 重试去重应该使用什么字段？

请提供 Go Channel SDK 的准确代码示例。

## Q3：Loading 卡片应该怎样实现

GitLink 查询可能需要数秒，不能在按钮回调内同步完成。

希望实现：

```text
用户点击“刷新”
-> 立即显示“正在读取 GitLink”
-> 后台完成查询
-> 更新为最终 PR 卡片
```

请确认推荐方式：

- 立即替换原卡片为 loading；
- 返回 Toast；
- 回复一条 loading 消息；
- 使用流式卡片；
- 使用异步卡片更新接口。

请说明哪种方式最适合企业自建应用机器人和 Channel SDK。

## Q4：固定卡片的准确更新方式

上一轮建议保存 `reply_message_id`，后续更新固定卡片，但 `channel.Send(... MsgID)` 是否真的表示“更新原消息”需要确认。

请明确：

1. 创建卡片使用哪个接口？
2. 回复原消息使用哪个接口？
3. 更新已发送卡片使用哪个接口？
4. 更新卡片是全量替换还是局部更新？
5. 是否只能更新机器人自己发送的卡片？
6. 一条卡片允许更新多少次？
7. 卡片多久后不能继续更新？
8. 原消息被撤回或删除后会返回什么错误？
9. 是否支持所有群成员看到同一个更新结果？
10. 是否支持按用户显示不同按钮状态？

## Q5：卡片尺寸和组件限制

上一轮回答称卡片正文存在长度限制，请给出准确的字符上限和超出后的行为。

```text
整张卡片最大 JSON 大小
单个 Markdown 元素最大长度
卡片最大元素数
单个 action 最大按钮数
button.value 最大大小
select_static 选项上限
卡片更新频率
单群机器人消息频率
```

同时确认日期选择应该使用哪个组件，而不是把 `select_static` 当作日期选择器。

## Q6：Base 是否存在唯一约束或原子 upsert

上一轮确认 Base 没有原子 upsert。请明确：Base 文本字段能否声明唯一约束？是否存在业务唯一键？

1. Base 文本字段能否声明唯一约束？
2. Base 是否存在业务唯一键？
3. 创建记录接口是否支持 `client_token` 或幂等键？
4. 批量创建接口是否支持幂等？
5. 搜索记录后创建是否存在并发竞态？
6. 是否支持条件更新或版本号比较？
7. 多实例并发写入时，官方推荐怎样避免重复记录？
8. Base 自动化的“查找—否则创建”是否是原子操作？

## Q7：Base 记录变更是否有事件订阅机制，以及如何安全同步到 SQLite

当前推荐模型是：

```text
Base = 管理界面
SQLite = 执行真源
```

请给出从 Base 到 Gateway 的成熟同步方式：

- Bot 定时轮询 Base；
- Base 记录变更事件；
- Base 自动化调用 Webhook；
- 飞书集成平台；
- 管理员点击卡片确认后由 Bot 写 SQLite。

需要回答：

1. Base 是否有记录新增、修改、删除事件订阅？
2. 能否知道具体修改人？
3. 能否获得修改前和修改后的字段？
4. 事件是否保证顺序？
5. 是否可能重复投递？
6. 删除记录后如何同步禁用绑定？
7. 如何避免 Base 编辑者绕过机器人审批扩大仓库范围？
8. Base 高级权限能否限制某角色只能修改 `enabled`，不能修改 `repository`？
9. Bot 如何验证 Base 修改者确实有管理员权限？

## Q8：`repo_allowlist` 使用单选/多选字段是否真的安全

上一轮建议将仓库列表配置为“只能选、不能填”的多选字段。

请确认：

1. 有记录编辑权的用户能否新增多选选项？
2. 谁可以修改字段选项集合？
3. 高级权限能否禁止修改字段结构，但允许编辑记录？
4. 如果一个管理员可以选择已有的所有仓库，如何限制他只能管理部分仓库？
5. 是否应该改用关联表：

```text
Repositories
Bindings
BindingRepositories
```

6. Base 是否支持按行限制管理员只能修改自己负责的仓库绑定？

## Q9：应用身份创建的 Base/Doc/Task 的 owner 归属、协作者管理和停用后的资源处理

当前准备采用应用身份创建资源。

请给出完整流程：

```text
应用创建 Base/Doc/Task
-> 添加 Owner 为协作者
-> 设置 Owner 权限
-> 设置团队权限
-> 应用权限回收或应用停用后的资源处理
```

请回答：

1. 应用身份创建的资源真正 owner 是谁？
2. 能否把资源所有权转移给用户？
3. 转移后应用是否仍能继续同步？
4. 应用被停用、删除或 Secret 轮换后资源是否保留？
5. 如何让资源出现在 Owner 的云空间或文档列表中？
6. Base、Doc、Task 添加协作者分别使用什么接口和权限？
7. 应用身份创建和用户身份创建在企业审计中显示的操作者有什么区别？

## Q10：飞书 OAuth 对 GitLink 账号绑定是否必要

机器人收到可信的飞书消息事件时，已经获得该应用范围内的发送者 `open_id`。

因此请重新评估：

```text
飞书 OAuth 是否真的能证明 GitLink 身份？
```

它似乎只能再次证明用户的飞书身份，不能证明该用户拥有某个 GitLink 账号。

请比较：

A. 飞书 `open_id` + GitLink OAuth
B. 飞书 `open_id` + GitLink 一次性绑定码
C. 飞书 OAuth + 手工输入 GitLink 用户名
D. 飞书 OAuth + GitLink OAuth 双重授权

请明确：

1. 哪一步证明飞书身份？
2. 哪一步证明 GitLink 账号所有权？
3. 是否完全可以不使用飞书 OAuth？
4. 如果不以用户身份调用飞书 OpenAPI，是否无需保存飞书用户 Token？
5. 哪种流程最符合最小权限原则？

## Q11：群主和群管理员的能力边界

上一轮确认目前只能稳定获取群主，不能读取完整群管理员列表。

请进一步回答：

1. `im.chat.get` 返回的群主字段准确叫什么？
2. 应申请哪个当前有效 Scope？
3. 群主转让后是否有事件通知？
4. Bot 是否能订阅群信息变化？
5. 如何及时使旧群主失去仓库绑定管理权限？
6. 是否有可读取“当前用户是否为群管理员”的接口，即使不能列出所有管理员？
7. 飞书审批是否适合替代群管理员识别？
8. 能否由企业应用管理员在管理后台维护机器人管理员角色？

## Q12：Base 自动化是否适合安全配置变更

上一轮建议通过 Base 自动化同步配置和提醒。

请明确 Base 自动化的工程边界：

```text
触发延迟
执行次数配额
失败重试
失败告警
执行日志
重复触发
顺序保证
跨表事务
Webhook 鉴权
不同飞书版本的能力差异
```

特别需要确认：

- Base 自动化是否保证幂等；
- “查找—否则创建”是否可能创建重复记录；
- 自动化是否适合权限敏感的仓库绑定；
- 是否更适合只处理通知，不处理安全配置。

## Q13：Task v2 应该由谁拥有以及如何幂等更新

每个 PR 最多创建一个 Review Task。

请给出：

1. 创建 Task v2 的准确接口和 Scope；
2. 应用身份创建的任务负责人如何设置为用户；
3. 如何添加关注人；
4. 如何设置日期而不是 UTC 零点时间；
5. 如何更新截止日期；
6. 如何标记完成；
7. 如何关闭或归档已 merged/closed PR 的任务；
8. 是否支持业务幂等键；
9. 没有幂等键时应该保存哪个 Task GUID；
10. Task 删除后再次同步如何识别；
11. Task 原生提醒与 Base 自动化提醒是否会重复。

## Q14：甘特图字段设计是否完整

上一轮只给出一个 `due_date`，但甘特图通常需要开始时间和结束时间。

请确认飞书 Base 甘特视图需要：

```text
start_date
end_date
duration
milestone
dependency
```

中的哪些字段。

PR Review Queue 是否更适合：

- 甘特视图；
- 看板视图；
- 日历视图；
- 时间线；
- 仅使用 Task 截止日期？

请避免为了展示甘特图而虚构没有真实意义的开始日期。

## Q15：人员字段如何写入

Base 的 `assigned_to` 是人员字段，当前机器人只有消息发送者的 `open_id`。

请确认：

1. 人员字段写入接受 `open_id`、`user_id` 还是 `union_id`？
2. API 查询参数 `user_id_type` 是否影响人员字段值？
3. 外部用户能否写入人员字段？
4. 应用可用范围是否影响写入？
5. 用户离职后字段值会怎样变化？
6. 是否需要额外通讯录权限？
7. 是否可以在不读取姓名、手机号和邮箱的情况下写入人员字段？

## Q16：事件去重到底使用 `message_id` 还是 `event_id`

我们第一轮回答中错误地建议同时使用 `event_id` 作为去重键。根据飞书官方文档，
`im.message.receive_v1` 应使用 `message_id`。请确认以下各事件的正确幂等键：

```text
im.message.receive_v1
卡片按钮回调
Base 记录变更
Task 状态变更
Doc 评论事件
```

并回答：

1. 同一消息重试时 `event_id` 是否可能变化？
2. 同一 `message_id` 是否可能对应不同编辑版本？
3. 卡片按钮连续点击如何区分重试和两次真实操作？
4. 每种事件的推荐去重 TTL 是多少？
5. 飞书最长重试周期是多少？

参考：<https://open.feishu.cn/document/server-docs/im-v1/message/events/receive?lang=zh-CN>

## Q17：多实例长连接的官方推荐模式

上一轮建议使用 leader election，只让一个实例保持活动。但这样实际上失去了多个长连接随机分发带来的可用性。

请比较：

A. 只有 Leader 建立飞书长连接
B. 所有实例建立长连接，共享数据库去重和任务队列
C. 单连接接入进程加独立 Worker 集群
D. 主备两个连接，但备连接不处理事件

请给出官方更推荐的生产拓扑，并说明：

- SDK 自动重连时是否可能重复事件；
- 单应用多个连接是否保证随机均衡；
- 一个连接断开后是否会立即转移；
- 是否存在连接保活或健康状态事件；
- 如何优雅关闭连接；
- 是否建议将 WebSocket 接入层与业务 Worker 分离。

## Q18：真实平台验收应保存哪些脱敏证据

请给出飞书官方建议的验收证据清单，至少覆盖：

```text
事件类型
事件或消息幂等标识的哈希
handler latency
ACK 结果
卡片回调结果
发送/回复/更新消息 ID 的哈希
OpenAPI request_id
错误码
Base record_id 哈希
Doc document_id 哈希
Task GUID 哈希
应用版本
权限 Scope 版本
```

请说明哪些字段适合日志记录，哪些字段属于敏感信息，不能进入日志或比赛材料。

## 期望的四项交付物

### 1. 准确的最小权限 Scope 清单

验收标准：按场景分组；使用准确的 `scope_name`；注明是应用身份还是用户身份；注明是否
需要发布新版本及是否需要管理员审批。

### 2. 卡片回调、异步处理和更新原卡片的 Go SDK 示例

验收标准：使用当前 Channel SDK；示例必须包含“ACK 立即返回”和“异步卡片更新”两个
步骤，并标出需要持久化的消息或卡片标识。

### 3. Base 作为管理界面、SQLite 作为真源的事件同步方案

验收标准：说明 Base 是否具有记录变更事件订阅；如果没有，给出定时轮询的实现边界、
游标、水位、重复处理和删除同步方案。

### 4. Base、Doc、Task 的真实首次写入与幂等验收步骤

验收标准：给出每一步的 OpenAPI 接口名和准确 Scope；覆盖首次创建、重复执行、远端成功
但本地保存失败、资源被人工删除后的处理；不要求提供完整代码。
